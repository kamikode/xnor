#![no_std]
extern crate alloc;

pub mod old_array;
pub mod old_shape;

pub mod prelude {
    /*
    pub use crate::old_array::{
        Array, BoolArray, ConvertibleToArray, F32Array, F64Array, I128Array, I16Array, I32Array,
        I64Array, I8Array, U128Array, U16Array, U32Array, U64Array, U8Array,
    };
    pub use crate::old_shape::Shape;
    */
}
